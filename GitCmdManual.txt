ssh-keygen -C 'xiakun@esafenet.com' -t rsa

ssh –T git@github.com

git add ReadMe.txt

git commit –m “first commit”

git status
git diff ReadMe.txt
git status -s

git log
git log --pretty=oneline

git reset --hard HEAD^
git reflog
git reset --hard a9d9e9077c3 

git checkout -- ReadMe.txt

git rm ReadMe.txt
git commit -m "rm file"
git push origin master

git clone git@github.com:xiakun-esafenet/MyCode.git
git remote add origin https://github.com/xiakun-esafenet/ACE_Example_Codes.git
git push origin master
git pull

git checkout -b dev	#创建并进入dev分支，相当于下面两条命令的组合
git branch dev
git checkout dev

git checkout master	#回到主分支

git branch	#查看当前分支
	
git branch –d dev	#删除分支dev

git merge dev	#首先要运行到需要合并的分析，例如主干master，然后再执行上述命令将dev分支的内容合并到主干上。